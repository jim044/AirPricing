<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:int="http://www.springframework.org/schema/integration"
	   xmlns:int-webflux="http://www.springframework.org/schema/integration/webflux"
	   xsi:schemaLocation="http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration/webflux
    	https://www.springframework.org/schema/integration/webflux/spring-integration-webflux.xsd">

	<int:channel id="requestTokenAmadeusChannel"/>

	<int:inbound-channel-adapter channel="requestTokenAmadeusChannel" expression="'getRequests'">
		<int:poller fixed-rate="10000"/>
	</int:inbound-channel-adapter>

	<int:chain input-channel="requestTokenAmadeusChannel" output-channel="nullChannel">

		<int:transformer ref="flightOffersTransformer"/>

		<int:object-to-json-transformer />

		<int-webflux:outbound-gateway id="requestTokenAmadeus"
											  web-client="amadeusWebClient"
											  url-expression="'${application.amadeus.base-path}' + '${application.amadeus.endpoint-flight-offers}'"
											  http-method="POST"
									  charset="UTF-8"
									  expected-response-type="java.lang.String">
		</int-webflux:outbound-gateway>

		<int:transformer ref="transformerTest"/>
	</int:chain>


</beans>